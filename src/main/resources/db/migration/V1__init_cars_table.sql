CREATE TABLE cars(
                     id int primary key,
                     model varchar(50) not null,
                     car_make varchar(50) not null,
                     colour varchar(30) null,
                     year int null
)
